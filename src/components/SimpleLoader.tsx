import { Box, CircularProgress } from "@mui/material";

const SimpleLoader = () => {
  return (
    <Box textAlign="center">
      <CircularProgress />
    </Box>
  );
};

export default SimpleLoader;
